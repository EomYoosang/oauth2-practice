# OAuth2 다중 제공자 로그인 PRD

## 1. 개요
본 Spring Boot 애플리케이션에 애플·구글·카카오 OAuth2 로그인과 이메일/비밀번호 기반 로컬 로그인을 모두 제공하며, 인증은 JWT 액세스 토큰과 리프레시 토큰, Redis 기반 토큰 스토리지를 사용해 세션리스 방식으로 운영합니다. 기능은 통합된 도메인 모델과 공통 보안 구성을 통해 사용자 온보딩과 인증 경험을 일관되게 제공합니다.

## 2. 목표 및 성공 지표
- 단일 릴리스 범위 내에서 네 가지 로그인 수단(애플, 구글, 카카오, 이메일/비밀번호)을 모두 지원합니다.
- 성공 로그인 100%에 대해 표준화된 사용자 프로필(프로바이더 ID, 이메일, 표시 이름, 아바타 등)을 `User` 엔티티로 저장하고, 관련 계정 엔티티와 연결합니다.
- 애플리케이션 결함으로 인한 인증 실패율을 30일 기준 1% 미만으로 유지합니다.
- 각 로그인 경로에 대해 토큰 발급·갱신을 포함한 종단 간 자동화 테스트를 제공하고, Redis 기반 리프레시 토큰 무결성을 검증합니다.

## 3. 가정 및 의존성
- 애플리케이션은 Java 17, Spring Boot 3.5.6, Spring Security OAuth2 Client, Spring Security PasswordEncoder를 사용합니다.
- 데이터베이스: MySQL 8.x
- JWT 서명은 대칭 키(HMAC) 또는 공개키 기반(RSA)을 환경 변수로 주입해 관리하며, 키 교체 전략은 별도 문서화합니다.
- Redis 인스턴스를 인증 서비스에서 접근 가능해야 하며, 리프레시 토큰과 블랙리스트 처리된 액세스 토큰을 저장합니다.
- 제공자 자격 증명(클라이언트 ID/비밀, 리다이렉트 URL)은 환경 변수 또는 시크릿 매니저로 주입하고 저장소에 포함하지 않습니다.
- 통합 실패 이벤트 처리는 RabbitMQ 브로커(Dead Letter Queue 포함)와 Slack Webhook 알림을 연계해 운영합니다.
- 이메일 검증 및 비밀번호 재설정 메일 발송은 SMTP 인터페이스를 추상화해 구현하며, 초기 후보로 네이버 메일을 검토합니다.
- CI/CD 파이프라인은 온프레미스 Jenkins를 사용해 배포 자동화를 제공합니다.

## 4. 사용자 스토리
- 일반 사용자로서 이메일과 비밀번호로 로그인해 소셜 계정 없이도 서비스를 이용하고 싶습니다.
- 구글 사용자로서 구글 계정으로 로그인해 별도 비밀번호를 만들고 싶지 않습니다.
- iOS 중심 사용자로서 애플 계정으로 로그인해 애플 생태계 내 자격 증명을 유지하고 싶습니다.
- 카카오 사용자로서 카카오톡 계정으로 인증해 기존 프로필을 재사용하고 싶습니다.
- 관리자 입장에서 어떤 로그인 방식이 가장 많이 사용되는지 확인해 향후 투자 우선순위를 정하고 싶습니다.

## 5. 기능 요구사항
### 5.1 공통 플로우
- `/login` 페이지와 모바일/웹 클라이언트에서 이메일/비밀번호 및 각 OAuth2 제공자 버튼을 노출하며, 순서는 설정으로 조정 가능합니다.
- 성공 인증 후 JWT 액세스 토큰(만료 15분 기본)과 리프레시 토큰(만료 7일 기본)을 발급합니다.
- 발급된 액세스 토큰은 HTTP 헤더 `Authorization: Bearer <token>` 형태로 사용하며, 리프레시 토큰은 HTTPS-only 쿠키 또는 보안 저장소에 전달합니다.
- OAuth2에서 수신한 속성은 내부 `User` 엔티티에 정규화하고, `User`와 각 계정 엔티티를 1:N 관계로 매핑합니다.
- 최초 로그인과 재로그인 시 제공자별 프로필 데이터를 갱신하되 로컬 사용자 설정은 유지합니다.

### 5.2 이메일/비밀번호 로그인
- 이메일은 중복되지 않아야 하며, 비밀번호는 BCrypt 기반 해시를 사용합니다.
- 회원가입 시 이메일 검증 토큰을 발송하고, 검증 완료 후 로그인 허용합니다.
- 5회 연속 실패 시 일정 시간 로그인 시도를 제한하고 Redis에 잠금 상태를 저장합니다.

### 5.3 애플 로그인
- 클라이언트 시크릿 JWT를 생성하는 인가 코드 플로우를 사용하며, JWT는 최소 5분마다 갱신합니다.
- `name`, `email` 스코프를 요청하고, 최초 로그인 시 반환되는 전체 이름을 `User` 엔티티에 저장합니다.
- Sign in with Apple JS 위젯을 통합할 경우 지원하며, 그렇지 않으면 리다이렉트 방식을 사용합니다.

### 5.4 구글 로그인
- Spring Security가 PKCE를 강제하는 인가 코드 플로우를 사용합니다.
- `openid email profile` 스코프를 요청해 ID 토큰을 확보합니다.
- `GOOGLE_ALLOWED_DOMAIN` 환경 변수가 설정된 경우 해당 도메인 이외의 이메일은 등록/로그인을 차단합니다.

### 5.5 카카오 로그인
- REST API 키를 사용하는 OAuth 인가 코드 플로우를 구현합니다.
- `account_email`, `profile_nickname` 스코프를 요청하고, 검증된 이메일이 없으면 사용자에게 입력을 요청합니다.
- 제공된 프로필 이미지는 내부 아바타 URL에 매핑합니다.

### 5.6 계정 및 사용자 모델
- `User` 엔티티는 공통 사용자 정보를 관리하며, 이메일 인증 여부, 표시 이름, 아바타 URL, 기본 역할을 포함합니다.
- `EmailAccount`, `AppleAccount`, `GoogleAccount`, `KakaoAccount` 엔티티는 각각 자격 증명과 제공자별 메타데이터(제공자 ID, 갱신 토큰 등)를 보관합니다.
- 각 계정 엔티티는 `User`와 단방향 또는 양방향 연관관계를 맺으며 유일성(제공자 ID 기준)을 보장합니다.
- 동일 이메일이 확인된 소셜 계정이 로그인하면 기존 `EmailAccount`와 자동 매칭하고, 사용자 동의를 받은 뒤 단일 `User`로 통합합니다. 통합 실패 시 전체 동작을 롤백하고 `ACCOUNT_LINK_FAILED` 오류 메시지를 응답하며 기존 로그인 상태를 유지합니다.

### 5.7 토큰 발급 및 폐기
- 액세스 토큰은 서명된 JWT로 발급하며, 사용자 ID, 권한, 제공자 타입, 토큰 ID(jti)를 포함합니다.
- 리프레시 토큰은 식별자(UUID)와 메타데이터(사용자 ID, 제공자, 만료 시각)를 Redis에 저장하고 TTL을 설정합니다.
- 로그아웃, 비밀번호 변경, 계정 연결 해제 시 해당 리프레시 토큰 레코드를 삭제하고, 필요한 경우 액세스 토큰 jti를 블랙리스트에 추가합니다.
- 리프레시 토큰 재발급 시 기존 토큰을 즉시 폐기하는 1회용 정책을 적용합니다.

## 6. UX 및 상호작용
- 로그인 화면에서 선택한 제공자에 따라 명확한 로딩 상태와 에러 메시지(`Apple 인증 실패`)를 제공합니다.
- 이메일 로그인 실패 시 남은 시도 횟수를 안내하며, 잠금 상태일 경우 잠금 해제 절차를 안내합니다.
- 동일 이메일 소셜 계정이 감지되면 계정 통합 안내 모달을 노출합니다. 동의 절차 중 오류가 발생하면 명확한 오류 메시지와 재시도 옵션을 제공하며, 기존 로그인 정보는 변경하지 않습니다.
- 토큰 만료로 인해 인증이 필요한 경우, 자동으로 리프레시 토큰을 사용해 재발급하거나 재로그인을 요청합니다.

## 7. 오류 처리 및 엣지 케이스
- 제공자 타임아웃은 재시도 안내와 함께 처리하고, 반복 실패 시 로그를 기반으로 경고를 발송합니다.
- state/nonce 검증 실패, JWT 서명 검증 실패는 WARN 이상으로 로깅하고 일반 보안 오류 메시지를 노출합니다.
- 계정 통합 시 어떤 단계에서든 오류가 발생하면 트랜잭션을 롤백해 데이터 정합성을 유지하고, `ACCOUNT_LINK_FAILED` 오류 코드와 사용자 친화적 메시지를 반환합니다.
- Redis 장애 시 임시적으로 로그인 요청을 차단하고, 운영 알림을 트리거하며, 장애 복구 전략을 문서화합니다.

## 8. 비기능 요구사항
- 인증 API는 95퍼센타일 기준 3초 이내 응답해야 하며, Redis 왕복을 포함한 토큰 발급 경로를 최적화합니다.
- 인증 관련 엔드포인트 가용성을 99.5% 이상 유지합니다.
- JWT에는 최소 권한만 포함하고, 민감 정보(비밀번호 해시 등)는 토큰에 포함하지 않습니다.
- 토큰 키와 OAuth 자격 증명은 Secret Manager 또는 Vault로 관리합니다.

## 9. 분석 및 모니터링
- Micrometer로 로그인 성공/실패, 토큰 재발급 횟수, 잠금 발생 건수, 계정 통합 성공/실패 측정치를 계측합니다.
- `/actuator/metrics/oauth2.login.success`, `.failure`, `/actuator/metrics/auth.refresh.success`, `/actuator/metrics/account.link.failure` 등을 노출해 대시보드화합니다.
- 실패 알림은 Slack Webhook과 고객센터 티켓 시스템에 연동해 운영팀이 즉시 대응할 수 있도록 구성합니다.

## 10. 미결 사항 및 위험

### 10.1 계정 통합 실패 대응
- 위험 요약: 자동 통합 단계에서 외부 OAuth 제공자 오류나 내부 데이터 무결성 문제로 플로우가 중단될 수 있음.
- 해결 방안:
  - 통합 플로우에 Resilience4j `Retry`(최대 3회, 지수형 백오프)와 `CircuitBreaker`(5분 쿨다운)를 적용해 일시 오류를 흡수합니다.
  - RabbitMQ에 `integration.events` 익스체인지와 DLQ(`integration.events.dlq`)를 구성하고, DLQ 적체 시 Jenkins 잡으로 재처리 배치를 실행합니다.
  - 실패 메시지를 Slack Webhook과 고객센터 티켓 시스템(예: JIRA Service Management)에 전송해 수동 지원을 즉시 시작할 수 있도록 합니다.
  - 운영 Runbook에 수동 계정 통합 절차(데이터 정합성 점검, 재시도 조건, 사용자 공지 템플릿)를 문서화합니다.
- 책임/일정: 인증 팀이 2단계 스프린트 내에 Resilience4j/RabbitMQ 구성을 완료하고, 운영팀은 스프린트 종료 전 Runbook과 Slack 연동을 검증합니다.

### 10.2 Redis 장애 대비
- 위험 요약: Redis 장애 시 리프레시 토큰 관리가 불가능해지고 로그인 요청이 실패할 수 있음.
- 해결 방안:
  - Redis Sentinel 3노드 구성을 표준으로 하고, 헬스체크 실패 시 애플리케이션이 Autowired `TokenStore` 를 MySQL 8 기반 폴백 구현으로 즉시 전환하도록 합니다.
  - MySQL 폴백 테이블은 TTL 컬럼과 상태 플래그를 포함하며, 복구 후 Jenkins 스케줄 잡이 Redis와 MySQL 데이터를 양방향 비교해 정리합니다.
  - 장애 발생 시 Slack 알림 + 운영 대시보드(InfluxDB/Grafana)에서 폴백 상태를 노출하고, 복구 후 영향 사용자에게 강제 재로그인 공지를 발송합니다.
  - 월 1회 DR 리허설(폴백 전환/복구)을 실행해 Runbook의 정확성을 검증합니다.
- 책임/일정: 플랫폼 팀이 1단계 종료 전 Sentinel 및 폴백 구현을 완료하고, 운영팀이 분기별 DR 리허설 일정을 관리합니다.

### 10.3 메일 발송 인프라 확보
- 위험 요약: 이메일 검증/비밀번호 재설정 기능이 안정적인 SMTP 인프라에 의존.
- 해결 방안:
  - `MailGateway` 인터페이스와 `NaverSmtpClient` 초안을 구현하고, 공급자 전환을 위한 설정 레이어를 분리합니다.
  - 네이버 SMTP 가용성/전송량 제한을 검증하며, 필요 시 백업 공급자(Postmark 등) 전환 스위치를 준비합니다.
  - 발송 성공률, 지연 시간, 실패율을 Slack 알림 및 Grafana 대시보드에 노출하고 SLA(99% 성공, 5분 이내) 위반 시 알람을 발송합니다.
  - 대량 발송 시 큐( RabbitMQ `mail.dispatch` )를 통해 버퍼링하여 SMTP 제한을 우회하도록 설계합니다.
- 책임/일정: 백엔드 팀이 1단계 내 인터페이스와 모듈을 구현하고, 운영팀이 2단계 전까지 모니터링/알림 구성을 마칩니다.

### 10.4 키 보관 및 로테이션 자동화
- 위험 요약: 애플 Private Key 및 JWT 서명 키 로테이션 미비로 보안 사고 위험이 존재.
- 해결 방안:
  - HashiCorp Vault 또는 온프레미스 HSM에 키를 저장하고, 접근은 Jenkins Credentials Provider와 RBAC 정책으로 제한합니다.
  - Jenkins 파이프라인에 90일 주기 스케줄을 설정해 새 키 생성 → 검증용 스테이징 배포 → 프로덕션 반영 순으로 자동화합니다.
  - 키 교체 후 JWKS 엔드포인트, 애플 클라이언트 시크릿 재생성 스크립트, 롤백 절차를 자동 실행하며, 결과를 Slack 알림으로 공유합니다.
  - 반기마다 키 로테이션 드릴(모의 실행)을 진행해 Runbook의 정확성과 접근 권한을 점검합니다.
- 책임/일정: 보안팀이 3단계 시작 전 Vault/HSM 구성을 완료하고, DevOps 팀이 스프린트 3 내 Jenkins 파이프라인과 알림 연계를 구현합니다.

## 11. 단계별 롤아웃 계획
- 1단계: 이메일/비밀번호 로그인과 JWT/리프레시 토큰, Redis 인프라 구축 및 테스트.
- 2단계: 구글 로그인 통합 및 토큰 발급 흐름 검증.
- 3단계: 애플 로그인 구현(클라이언트 시크릿 자동화 포함).
- 4단계: 카카오 로그인 통합, UX 개선, 계정 통합 정책 정비.
- 5단계: 보안 리뷰, 부하 테스트, 프로덕션 배포.
