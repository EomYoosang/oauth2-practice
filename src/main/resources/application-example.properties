# Copy to application.properties (ignored) for local overrides
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# Default mail properties – configure via environment variables for production
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_USERNAME=
MAIL_PASSWORD=
VERIFICATION_BASE_URL=http://localhost:8080/verify-email?token=
VERIFICATION_EXPIRATION_MINUTES=60
VERIFICATION_FROM_NAME=OAuth2 Practice
