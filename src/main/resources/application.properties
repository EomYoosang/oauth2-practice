spring.application.name=oauth2_practice
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false

# Mail configuration (set via environment variables in non-local environments)
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=dbtkd021801@gmail.com
spring.mail.password=dejorekhwreghesr
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Email verification settings
app.auth.verification.base-url=${VERIFICATION_BASE_URL:http://localhost:8080/verify-email?token=}
app.auth.verification.expiration-minutes=${VERIFICATION_EXPIRATION_MINUTES:60}
app.auth.verification.from-name=${VERIFICATION_FROM_NAME:OAuth2 Practice}
